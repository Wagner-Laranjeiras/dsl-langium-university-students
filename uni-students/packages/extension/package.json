{
    "name": "vscode-uni-students",
    "description": "The extension specific package",
    "version": "0.0.1",
    "displayName": "uni-students",
    "type": "module",
    "engines": {
        "vscode": "^1.67.0"
    },
    "categories": [
        "Programming Languages"
    ],
    "contributes": {
        "languages": [{
            "id": "university-students",
            "aliases": ["University Students", "university-students"],
            "extensions": [".unis"],
            "configuration": "./language-configuration.json"
        }],
        "grammars": [{
            "language": "university-students",
            "scopeName": "source.university-students",
            "path": "syntaxes/university-students.tmLanguage.json"
        }]
    },
    "activationEvents": [
        "onLanguage:university-students"
    ],
    "main": "./out/extension/main.cjs",
    "scripts": {
        "clean": "shx rm -fr *.tsbuildinfo out syntaxes",
        "vscode:prepublish": "npm run build && npm run --include-workspace-root --workspace=../.. lint",
        "build:prepare": "shx mkdir -p ./syntaxes/ && shx cp -f ../language/syntaxes/university-students.tmLanguage.json ./syntaxes/university-students.tmLanguage.json",
        "build": "npm run build:prepare && tsc -b tsconfig.json && node esbuild.mjs",
        "build:clean": "npm run clean && npm run build",
        "watch": "npm run build:prepare && concurrently -n tsc,esbuild -c blue,yellow \"tsc -b tsconfig.json --watch\" \"node esbuild.mjs --watch\""
    },
    "dependencies": {
        "university-students-language": "0.0.1",
        "vscode-languageclient": "~9.0.1",
        "vscode-languageserver": "~9.0.1"
    },
    "devDependencies": {
        "@types/vscode": "~1.67.0",
        "concurrently": "~8.2.1",
        "esbuild": "~0.25.5"
    },
    "vsce": {
      "dependencies": false
    }
}
